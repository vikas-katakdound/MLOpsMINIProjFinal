from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='mlops miniproject dagshub, mlflow, github, AWS s3, web app',
    author='adi',
    license='',
)
